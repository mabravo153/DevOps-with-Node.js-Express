version: "3.9"
services:
  node-app:
    build:
      context: .
      args:
        - NODE_ENV=development
    volumes:
      - ./:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - MONGO_USER=mabravo153
      - MONGO_PASSWORD=contrasenaEjemplo
    command: npm run dev
